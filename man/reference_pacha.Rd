% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacha.R
\name{reference_pacha}
\alias{reference_pacha}
\title{Retrieve the source database citation}
\usage{
reference_pacha(
  species = NULL,
  language = NULL,
  source = NULL,
  refresh = FALSE
)
}
\arguments{
\item{species}{Ignored. Accepted only so a positional call does not
collide with \code{language}. Unlike the other accessors in this module,
\code{reference_pacha()} does not return a species-specific citation: the
citation identifies the configured dataset or archive as a whole, and
is identical for every species queried against that source. Passing a
non-\code{NULL} value here has no effect on the result and emits a warning.}

\item{language}{Optional output-language code or alias used for the
localized no-data/connection message.}

\item{source}{Optional source override: \code{"api"}/\code{"web"} or
\code{"coldp"}/\code{"local"}. \code{NULL} uses the configured default.}

\item{refresh}{Logical scalar. If \code{TRUE}, fetches the citation again
instead of reusing its cached value.}
}
\value{
A single character string with the plain-text source citation, or
the localized no-data/connection message.
}
\description{
Retrieves how the configured data source itself should be cited, as
distinct from any per-species bibliographic reference. For the API
source, this is the \code{citation} field of the configured ChecklistBank
dataset (\code{GET /dataset/{key}}), resolved once and cached, since it does
not vary by species. For the local source, this is the \code{citation} entry
of \code{metadata.yaml} inside the configured ColDP archive. Either source may
return the citation as CSL HTML (e.g. wrapped in \verb{<div class="csl-entry">},
with \verb{<span style="font-style: italic">} and HTML entities); this function
strips all markup and decodes entities, returning clean, unformatted text.
}
\examples{
\dontrun{
reference_pacha()
reference_pacha(source = "coldp")
}
}
