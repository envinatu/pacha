% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacha.R
\name{common_names_pacha}
\alias{common_names_pacha}
\alias{common_names_pacha_md}
\title{Retrieve common names}
\usage{
common_names_pacha(species, language = NULL, source = NULL, refresh = FALSE)

common_names_pacha_md(species, language = NULL, source = NULL, refresh = FALSE)
}
\arguments{
\item{species}{Character scalar containing at least a genus and specific
epithet.}

\item{language}{Optional output-language code or alias used for labels.}

\item{source}{Optional source override: \code{"api"}/\code{"web"} or
\code{"coldp"}/\code{"local"}. \code{NULL} uses the configured default (\code{"api"}).}

\item{refresh}{Logical scalar. If \code{TRUE}, fetches the selected source again
instead of reusing its cached record.}
}
\value{
\code{common_names_pacha()} prints and invisibly returns a single
character string of names grouped by language, or the localized
no-data/connection message. \code{common_names_pacha_md()} returns, invisibly,
the emitted Markdown string, or \code{""} when no names are available.
}
\description{
Retrieves vernacular names for a scientific name from the currently
configured source. The API source queries the configured ChecklistBank
dataset; the local source reads \code{VernacularName.tsv} from the configured
ColDP archive and, when a country column is present, keeps only the
entries matching \code{coldp_country}. Names are grouped by language in the
returned value. The examples below use the Ecuadorian checklist dataset
configured by default, but the same calls work against any compatible
ChecklistBank dataset or ColDP archive.
}
\examples{
\dontrun{
common_names_pacha("Bidens andicola")
common_names_pacha("Bidens andicola", source = "coldp")
}
\dontrun{
common_names_pacha_md("Bidens andicola")
}
}
\seealso{
\link{accessors-pacha} for how plain-text and Markdown accessors work.
}
