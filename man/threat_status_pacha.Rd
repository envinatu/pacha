% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacha.R
\name{threat_status_pacha}
\alias{threat_status_pacha}
\alias{threat_status_pacha_md}
\title{Retrieve conservation threat status}
\usage{
threat_status_pacha(species, language = NULL, source = NULL, refresh = FALSE)

threat_status_pacha_md(
  species,
  language = NULL,
  source = NULL,
  refresh = FALSE
)
}
\arguments{
\item{species}{Character scalar containing at least a genus and specific
epithet.}

\item{language}{Optional output-language code or alias used to translate
the threat category and labels.}

\item{source}{Optional source override: \code{"api"}/\code{"web"} or
\code{"coldp"}/\code{"local"}. \code{NULL} uses the configured default (\code{"api"}).}

\item{refresh}{Logical scalar. If \code{TRUE}, fetches the selected source again
instead of reusing its cached record.}
}
\value{
\code{threat_status_pacha()} prints and invisibly returns a translated
conservation-status category, or the localized no-data/connection
message. \code{threat_status_pacha_md()} returns, invisibly, the emitted
Markdown string, or \code{""} when the source has no threat-status record.
}
\description{
Retrieves the conservation-status value documented for a taxon. The API
source reads the ChecklistBank taxon-distribution endpoint; the local
source reads \code{Distribution.tsv} using the same country-aware,
deterministic selection rule as \code{\link[=establishment_pacha]{establishment_pacha()}}. Typical values are
IUCN categories such as \code{EX}, \code{EW}, \code{CR}, \code{EN}, \code{VU}, \code{NT}, \code{LC}, \code{DD}, and
\code{NE}, though the function accepts any status recorded in the underlying
dataset.
}
\details{
Values are translated through the selected language dictionary's \code{threat}
mapping. Unmapped source values are returned in sentence case rather than
rejected, so alternative assessment schemes remain usable.
}
\examples{
\dontrun{
threat_status_pacha("Bidens andicola")
threat_status_pacha("Bidens andicola", source = "coldp", language = "en")
}
\dontrun{
threat_status_pacha_md("Bidens andicola", language = "en")
}
}
\seealso{
\link{accessors-pacha} for a detailed description of plain-text and Markdown accessors.
}
