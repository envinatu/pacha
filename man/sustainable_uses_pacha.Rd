% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacha.R
\name{sustainable_uses_pacha}
\alias{sustainable_uses_pacha}
\alias{sustainable_uses_pacha_md}
\title{Retrieve sustainable-use records}
\usage{
sustainable_uses_pacha(
  species,
  use = NULL,
  language = NULL,
  source = NULL,
  refresh = FALSE
)

sustainable_uses_pacha_md(
  species,
  use = NULL,
  language = NULL,
  source = NULL,
  refresh = FALSE
)
}
\arguments{
\item{species}{Character scalar containing at least a genus and specific
epithet.}

\item{use}{Optional category name or fragment, matched case-insensitively.
\code{NULL} returns every available category.}

\item{language}{Optional output-language code or alias used for labels.}

\item{source}{Optional source override: \code{"api"}/\code{"web"} or
\code{"coldp"}/\code{"local"}. \code{NULL} uses the configured default (\code{"api"}).}

\item{refresh}{Logical scalar. If \code{TRUE}, fetches the selected source again
instead of reusing its cached record.}
}
\value{
\code{sustainable_uses_pacha()} prints and invisibly returns a
\verb{Category:} heading followed by one indented use per line, for every
matching category, or the localized no-data/connection message.
\code{sustainable_uses_pacha_md()} returns, invisibly, the emitted Markdown
string, or \code{""} when no matching sustainable-use data are available.
}
\description{
Retrieves sustainable-use categories and values from the configured
source: the ChecklistBank taxon-property endpoint for the API source, or
\code{TaxonProperty.tsv} for the local source. In either case, categories
matching \code{use_exclude_pattern} are dropped. This keeps the function usable
with any compatible property-based dataset, while remaining well suited
to the Ecuadorian sustainable-use dataset used in the examples below.
}
\examples{
\dontrun{
sustainable_uses_pacha("Bidens andicola")
sustainable_uses_pacha("Bidens andicola", use = "medicinal", source = "coldp")
}
\dontrun{
sustainable_uses_pacha_md("Bidens andicola", use = "medicinal")
}
}
\seealso{
\link{accessors-pacha} for a detailed description of plain-text and Markdown accessors.
}
