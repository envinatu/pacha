% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacha.R
\name{pacha_configure}
\alias{pacha_configure}
\title{Configure the pacha data module}
\usage{
pacha_configure(
  dataset = NULL,
  base_url = NULL,
  source_name = NULL,
  language = NULL,
  fetcher,
  use_exclude_pattern = NULL,
  labels = NULL,
  source = NULL,
  coldp_zip_file = NULL,
  coldp_country = NULL,
  timeout = NULL
)
}
\arguments{
\item{dataset}{Character scalar. ChecklistBank dataset identifier used by
the default API adapter.}

\item{base_url}{Character scalar. HTTP(S) base URL of the ChecklistBank API.}

\item{source_name}{Character scalar identifying the web source in
connection warnings.}

\item{language}{Character scalar. A language-dictionary file stem or one
of its aliases. Controls labels and presentation, not the source data.}

\item{fetcher}{Optional custom web adapter. Pass \code{NULL} explicitly to
restore the built-in ChecklistBank adapter; omit this argument to leave
the current adapter unchanged.}

\item{use_exclude_pattern}{Character scalar regular expression identifying
taxon-property names that must not be treated as sustainable-use classes.}

\item{labels}{Optional named list of named character vectors overriding
the default labels by language.}

\item{source}{Default source: \code{"api"}/\code{"web"} or \code{"coldp"}/\code{"local"}.}

\item{coldp_zip_file}{Path to a local ColDP ZIP archive. It must contain
\code{NameUsage.tsv}; \code{VernacularName.tsv}, \code{TaxonProperty.tsv}, and
\code{Distribution.tsv} are read when present. \code{NULL} leaves the configured
path unchanged. The \code{pacha_COLDP_ZIP} environment variable is used lazily
when no path is set.}

\item{coldp_country}{ISO country code used to select country-specific
records from the local ColDP archive: vernacular names in
\code{VernacularName.tsv}, and the preferred distribution row in
\code{Distribution.tsv} for establishment and threat status. Defaults to
\code{"EC"}.}

\item{timeout}{Positive numeric HTTP timeout, in seconds, for web requests.}
}
\value{
A named configuration list, returned visibly when called with no
arguments, and invisibly after an update.
}
\description{
Gets or updates the configuration shared by every public accessor in this
module. The accessors are source-agnostic: they query any
ChecklistBank-compatible web API, or read any compatible local Catalogue
of Life Data Package (ColDP) archive, provided the source exposes name
usages and, where relevant, vernacular names, taxon properties, and
distributions. The defaults set here -- the ChecklistBank web API as the
active \code{source}, dataset \code{"313479"} (the Ecuadorian checklist of plants
susceptible to sustainable use), and \code{coldp_country = "EC"} -- identify the
dataset used in this file's \verb{\dontrun} examples; any other ChecklistBank
dataset or compatible ColDP archive can be configured in its place without
modifying the module itself.
}
\details{
\code{fetcher} preserves the existing extension contract for the web source. It
receives \verb{(species, config)} and must return a list with optional
\code{common_names}, \code{sustainable_uses}, \code{indexation_urls}, \code{establishment},
\code{threat_status}, \code{transport_error}, and \code{transport_messages} elements. A
custom fetcher applies only when \code{source = "api"}; \code{source = "coldp"}
always reads the archive. For example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{my_fetcher <- function(species, config) \{
  list(
    common_names = list(es = c("nombre comun")),
    sustainable_uses = list(Medicinal = c("uso medicinal registrado")),
    indexation_urls = c(source = "https://example.org/taxon/123"),
    establishment = "native",
    threat_status = "LC",
    transport_error = FALSE,
    transport_messages = character()
  )
\}
pacha_configure(fetcher = my_fetcher)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
pacha_configure()
pacha_configure(source = "coldp", coldp_zip_file = "C:/data/ColDP.zip")
pacha_configure(source = "api", dataset = "313479", language = "es")
}
}
