% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacha.R
\name{is_listed_pacha}
\alias{is_listed_pacha}
\title{Report whether a species is listed in the configured dataset}
\usage{
is_listed_pacha(
  species,
  language = NULL,
  source = NULL,
  refresh = FALSE,
  detailed = FALSE
)
}
\arguments{
\item{species}{Character scalar containing at least a genus and specific
epithet.}

\item{language}{Optional output-language code or alias used for labels.}

\item{source}{Optional source override: \code{"api"}/\code{"web"} or
\code{"coldp"}/\code{"local"}. \code{NULL} uses the configured default (\code{"api"}).}

\item{refresh}{Logical scalar. If \code{TRUE}, fetches the selected source again
instead of reusing its cached record.}

\item{detailed}{Logical scalar. If \code{TRUE} and the species is listed, the
concrete sustainable-use records are appended, one category per block
and one use per line, in plain text without Markdown formatting.}
}
\value{
Invisibly, the printed plain-text statement: the species name,
the localized listed/not-listed statement, and, when \code{detailed = TRUE}
and the species is listed, its sustainable uses.
}
\description{
Resolves a scientific name against the currently configured source
(\code{api} or \code{coldp}) and reports, as a plain-text console message, whether
the species is listed in the working dataset. The message is taken from
the active language configuration and can be customized through the
package configuration; if the required messages are unavailable for the
selected language, the function raises an error. The \code{language} argument
is resolved in the same way as for the other accessors in this module,
accepting either a language alias or a language code. As with those
accessors, a taxonomic match resolved from the local ColDP archive that
required disambiguation raises one or more additional \code{warning()}s.
}
\examples{
\dontrun{
is_listed_pacha("Bidens andicola")
is_listed_pacha("Bidens andicola", detailed = TRUE)
is_listed_pacha("Bidens andicola", language = "en", source = "coldp")
}
}
