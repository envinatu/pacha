% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacha.R
\name{compare_pacha}
\alias{compare_pacha}
\title{Compare the web API and local ColDP source}
\usage{
compare_pacha(
  species,
  component = "all",
  language = NULL,
  refresh = FALSE,
  print = TRUE
)
}
\arguments{
\item{species}{Character scalar containing at least a genus and specific
epithet.}

\item{component}{One or more components to compare. Use \code{"all"} (the
default) for the full comparison, or select \code{"common_names"},
\code{"sustainable_uses"}, \code{"indexation_urls"}, \code{"establishment"}, and/or
\code{"threat_status"} individually. Recognized aliases map to these
standard components as follows: \code{"names"} to \code{"common_names"};
\code{"uses"} to \code{"sustainable_uses"}; \code{"urls"} to \code{"indexation_urls"};
\code{"origin"} to \code{"establishment"}; and \code{"threat"} and \code{"conservation"}
to \code{"threat_status"}. Matching is case-insensitive and tolerant of
spaces or hyphens in place of underscores.}

\item{language}{Optional output-language code or alias retained in the
report metadata; the comparison itself is language-neutral.}

\item{refresh}{Logical scalar. If \code{TRUE}, forces both source records to be
refreshed before comparison.}

\item{print}{Logical scalar. If \code{TRUE}, prints the comparison result.}
}
\value{
Invisibly, a list with \code{species}, \code{language}, \code{components},
\code{summary}, \code{details}, \code{api_record}, \code{coldp_record}, and \code{compared_at}.
\code{summary} is a data frame with one row per selected component and its
agreement status.
}
\description{
Validates and compares the data returned by both sources for one species.
The comparison always queries the API and the local ColDP archive,
regardless of the configured default source. It normalizes case, accents,
punctuation, and whitespace before assessing agreement, while retaining
the original source values in the printed result and returned report.
Sustainable uses are compared as \code{category :: value} pairs, so a value
moved between categories is reported as a discrepancy rather than
overlooked.
}
\details{
The function prints a concise, component-by-component result by default
and returns the full machine-readable report invisibly. Status values are
\code{"identical"}, \code{"partial_overlap"}, \code{"discrepant"}, \code{"one_source_missing"},
\code{"both_missing"}, \code{"api_unavailable"}, and \code{"coldp_unavailable"}. With
\code{component = "all"}, it compares common names, sustainable uses,
indexation URLs, establishment status, and threat status.
}
\examples{
\dontrun{
compare_pacha("Bidens andicola")
compare_pacha("Bidens andicola", component = "sustainable_uses")
compare_pacha("Bidens andicola", component = c("origin", "threat"))
compare_pacha("Bidens andicola", component = c("names", "urls"), print = FALSE)
}
}
