% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacha.R
\name{establishment_pacha}
\alias{establishment_pacha}
\alias{establishment_pacha_md}
\title{Retrieve establishment status}
\usage{
establishment_pacha(species, language = NULL, source = NULL, refresh = FALSE)

establishment_pacha_md(
  species,
  language = NULL,
  source = NULL,
  refresh = FALSE
)
}
\arguments{
\item{species}{Character scalar containing at least a genus and specific
epithet.}

\item{language}{Optional output-language code or alias used to translate
the establishment category and labels.}

\item{source}{Optional source override: \code{"api"}/\code{"web"} or
\code{"coldp"}/\code{"local"}. \code{NULL} uses the configured default (\code{"api"}).}

\item{refresh}{Logical scalar. If \code{TRUE}, fetches the selected source again
instead of reusing its cached record.}
}
\value{
\code{establishment_pacha()} prints and invisibly returns a translated
establishment category, or the localized no-data/connection message.
\code{establishment_pacha_md()} returns, invisibly, the emitted Markdown
string, or \code{""} when the source has no establishment record.
}
\description{
Retrieves the documented establishment means of a taxon -- native,
introduced, cultivated, naturalized, and so on. The API source reads the
ChecklistBank taxon-distribution endpoint. The local source reads
\code{Distribution.tsv} and prefers the entry matching the configured
\code{coldp_country}; when several equally relevant records remain, it selects
the lowest ColDP identifier and reports this through a warning raised by
the plain-text accessor.
}
\details{
Values are translated through the selected language dictionary's
\code{establishment} mapping. Values absent from that mapping are returned in
sentence case rather than rejected, so datasets using additional
establishment categories remain usable.
}
\examples{
\dontrun{
establishment_pacha("Bidens andicola")
establishment_pacha("Bidens andicola", source = "coldp", language = "en")
}
\dontrun{
establishment_pacha_md("Bidens andicola", language = "en")
}
}
\seealso{
\link{accessors-pacha} for a detailed description of plain-text and Markdown accessors.
}
