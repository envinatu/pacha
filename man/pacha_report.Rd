% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacha_report.R
\name{pacha_report}
\alias{pacha_report}
\title{Generate a per-species Markdown report}
\usage{
pacha_report(
  species,
  language = NULL,
  source = NULL,
  use = NULL,
  refresh = FALSE,
  print = TRUE
)
}
\arguments{
\item{species}{Character vector of one or more scientific names, each
containing at least a genus and a specific epithet.}

\item{language}{Optional output-language code or alias, forwarded to
\code{\link[=common_names_pacha_md]{common_names_pacha_md()}} and \code{\link[=sustainable_uses_pacha_md]{sustainable_uses_pacha_md()}}.}

\item{source}{Optional source override (\code{"api"}/\code{"web"} or
\code{"coldp"}/\code{"local"}), forwarded to the same two functions. \code{NULL}
uses each function's configured default.}

\item{use}{Optional sustainable-use category filter, forwarded to
\code{\link[=sustainable_uses_pacha_md]{sustainable_uses_pacha_md()}}. \code{NULL} includes every category.}

\item{refresh}{Logical scalar. If \code{TRUE}, bypasses the cache of
\code{\link[=common_names_pacha_md]{common_names_pacha_md()}} and \code{\link[=sustainable_uses_pacha_md]{sustainable_uses_pacha_md()}} and
re-fetches those two components for every species.}

\item{print}{Logical scalar. If \code{TRUE} (the default), the combined
Markdown is emitted via \code{cat()}, as required in a
\code{results = "asis"} knitr chunk. If \code{FALSE}, the string is built and
returned without being printed.}
}
\value{
A character scalar with the combined Markdown for every
element of \code{species}, one block per species separated by a blank
line. Returned invisibly when \code{print = TRUE}, visibly when
\code{print = FALSE}.
}
\description{
Concatenates the Markdown output of \code{\link[=pacha_sc_full_name_md]{pacha_sc_full_name_md()}},
\code{\link[=common_names_pacha_md]{common_names_pacha_md()}}, and \code{\link[=sustainable_uses_pacha_md]{sustainable_uses_pacha_md()}} into a single
Markdown-formatted string, with one block per species in \code{species},
suitable for a \code{results = "asis"} knitr chunk in an automated report.
}
\details{
The heading for each species always comes from
\code{\link[=pacha_sc_full_name_md]{pacha_sc_full_name_md()}}, which takes no argument beyond \code{species} and
resolves against its own dataset and cache, independently of
\code{language} and \code{source}; see \code{?pacha_sc_full_name_md}. \code{language} and
\code{source} are forwarded to both \code{\link[=common_names_pacha_md]{common_names_pacha_md()}} and
\code{\link[=sustainable_uses_pacha_md]{sustainable_uses_pacha_md()}}; \code{use} is forwarded only to
\code{\link[=sustainable_uses_pacha_md]{sustainable_uses_pacha_md()}}; \code{refresh} is forwarded to both
\code{\link[=common_names_pacha_md]{common_names_pacha_md()}} and \code{\link[=sustainable_uses_pacha_md]{sustainable_uses_pacha_md()}}, but not to
\code{\link[=pacha_sc_full_name_md]{pacha_sc_full_name_md()}}, as scientific names rely on a separate cache
unaffected by this parameter.

Each component is produced by capturing, not re-implementing, the
output of the corresponding \verb{_md} accessor: its internal \code{cat()} call
is suppressed and its invisible return value is reused directly, so
the combined report is emitted exactly once regardless of the number
of species or components involved. As documented for the underlying
accessors, a component with no data for a given species contributes
nothing to that species' block: there is no "no data" placeholder or
empty heading, since each \verb{_md} function returns \code{""} in that case.
\code{\link[=pacha_sc_full_name_md]{pacha_sc_full_name_md()}} has no such empty case: it always renders a
heading, falling back to the requested name verbatim when resolution
fails.

A structurally invalid element of \code{species} (missing, or lacking at
least a genus and a specific epithet) raises an error via the same
validation used by the underlying accessors; species preceding the
invalid one in the vector are not processed once that error is
raised.
}
\examples{
\dontrun{
# Inside a knitr chunk with `results = "asis"`:
pacha_report("Bidens andicola")
pacha_report(c("Bidens andicola", "Bomarea multiflora"), language = "en")
pacha_report("Bidens andicola", source = "coldp", use = "medicinal")
}
}
